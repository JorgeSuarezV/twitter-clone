import styled from "styled-components";
import {Body} from "../body/Body";


export const ReactionContainer = styled.div`
  display: flex;
  flex-direction: row;
  gap: 24px;
  align-items: center;
`

export const ReactionCount = styled(Body)`
    margin-bottom: 0;
    margin-top: 0;
`