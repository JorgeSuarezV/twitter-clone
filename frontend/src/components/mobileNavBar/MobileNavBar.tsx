import React from "react";
import {NavBarContainer} from "./MobileNavBarStyles";

export type MobileNavBarProps = {
    children?: React.ReactNode;
}

export function MobileNavBar({children}: MobileNavBarProps) {
    return (
        <NavBarContainer>

        </NavBarContainer>
    )
}